# P6_EXTERNAL_BENCHMARK

Purpose: metadata-only staging area for external founder/operator doctrine and AI-agent architecture benchmark sources.

Current status: registry scaffold only. This change does not add raw files, processed text, chunks, embeddings, vector index records, KG records, or retrieval eval results.

Rules:
- P6 sources are operating analogies and architecture references.
- P6 sources are not Asperitas internal facts.
- License and terms review is required before source acquisition.
- Preserve source_id, priority, disclosure level, license status, and provenance in every downstream artifact.

Canonical registry: `00_ADMIN/source_registries/external_benchmark_source_registry.csv`
